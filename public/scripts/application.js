// Generated by CoffeeScript 1.6.1
var Game, Player, game,
  __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

Game = (function() {

  function Game(canvasId) {
    var resizeCanvas,
      _this = this;
    this.stage = new createjs.Stage(canvasId);
    this.players = [];
    console.log("Started the game.");
    resizeCanvas = function() {
      _this.stage.canvas.width = window.innerWidth;
      _this.stage.canvas.height = window.innerHeight;
      return _this.stage.update();
    };
    resizeCanvas();
    window.addEventListener('resize', resizeCanvas);
    createjs.Ticker.setFPS(30);
    createjs.Ticker.addEventListener("tick", function() {
      var player, _i, _len, _ref;
      _ref = _this.players;
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        player = _ref[_i];
        if (player.actions.movement.up === true) {
          player.bitmap.y -= 15;
        }
        if (player.actions.movement.down === true) {
          player.bitmap.y += 15;
        }
        if (player.actions.movement.left === true) {
          player.bitmap.x -= 15;
        }
        if (player.actions.movement.right === true) {
          player.bitmap.x += 15;
        }
      }
      return _this.stage.update();
    });
    document.onkeydown = function(e) {
      switch (e.which) {
        case 87:
          return game.players[0].actions.movement.up = true;
        case 83:
          return game.players[0].actions.movement.down = true;
        case 65:
          return game.players[0].actions.movement.left = true;
        case 68:
          return game.players[0].actions.movement.right = true;
      }
    };
    document.onkeyup = function(e) {
      switch (e.which) {
        case 87:
          return game.players[0].actions.movement.up = false;
        case 83:
          return game.players[0].actions.movement.down = false;
        case 65:
          return game.players[0].actions.movement.left = false;
        case 68:
          return game.players[0].actions.movement.right = false;
      }
    };
  }

  return Game;

})();

Player = (function(_super) {

  __extends(Player, _super);

  function Player(name) {
    console.log("" + name + " has joined.");
    this.name = name || "Anon";
    this.bitmap = new createjs.Shape();
    this.bitmap.graphics.beginFill("red").drawCircle(0, 0, 50);
    this.bitmap.x = 50;
    this.bitmap.y = 50;
    this.actions = {
      movement: {
        up: false,
        down: false,
        left: false,
        right: false
      }
    };
    game.stage.addChild(this.bitmap);
    game.players.push(this);
  }

  return Player;

})(Game);

game = new Game("gameCanvas");

new Player("Jackson", true);

if (typeof window !== "undefined" && window !== null) {
  window.Game = Game;
} else if ((typeof module !== "undefined" && module !== null ? module.exports : void 0) != null) {
  module.exports = Game;
}
